# Writing Setup Command

Configure session defaults for writing workflows.

**Usage:** `/writing-with-agents:writing-setup [--vault-path PATH] [--content-type TYPE]`

Sets vault path for knowledge capture, default content type, and SEO preferences that persist across commands in the current session.
